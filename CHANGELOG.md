# Changelog

所有重要变更记录于此。格式参考 Keep a Changelog。

## [7.1.0] - 2026-09-18

### 新增
- **长提示词拆分**：通宵批量新增「✂️ 长文拆分成多条」——把一段长故事/描述按内容拆成多条场景提示词（预览可编辑后替换或追加到批量列表）。配置了云端 LLM 时由 LLM 分镜式拆分（保留主体/动作/镜头/氛围，按叙事顺序），未配置或云端失败时按句读均衡拆分兜底
- 已结束的通宵批次面板新增「收起」按钮（记住该批次不再弹出，新批次自动恢复显示）

### 修复
- 已结束批次残留显示过期的「ComfyUI 连接中断，重试中」提示（重试计数在批次结束时未清零 + 前端仅应在运行中显示）

## [7.0.0] - 2026-09-18

### 新增
- **Turbo LoRA 加速档**：接入 Comfy-Org 官方蒸馏加速 LoRA（8 步 / 4 步，各 1.8GB，模型库一键下载到 loras 目录）；工作流新增 `LoraLoaderModelOnly` 节点，选用后步数自动锁定、与 TeaCache 加速缓存互斥（自动失效）；创作台 / 长片接龙 / 通宵批量全线可选用；ETA 实测分桶按 LoRA 维度区分
- **批量图生视频**：通宵批量支持每行提示词配一张首帧图（第 i 张图对应第 i 行，图少的行自动退化为文生视频）；图片按批次目标画幅统一裁剪上传，保证全批同尺寸可无损拼接
- **批次自动混剪**：通宵批量新增「跑完自动拼接成合集」，全部完成后无损拼接为一条合集长片入库（抽取通用拼接助手与手动拼接共用）
- **作品库容量管理**：按分类（生成/插帧/超分/拼接）统计数量与体积；两步式清理（预览待删数量与体积 → 确认执行），固定保护星标与最近 7 天作品

### 修复
- build_workflow 中 Turbo LoRA 与 TeaCache 的互斥在节点层强制（此前仅调用方约定）

## [6.0.0] - 2026-09-18

### 新增
- **通宵批量**：多行提示词（每行一条，≤60 行）× 每条 1/2/4 个种子，单批最多 240 个任务无人值守连续生成，成片自动入库
- **断点续跑**：批次状态与剩余队列持久化（data_dir/batch.json），面板重启后自动续跑；重启前的在途任务由新进程收尾入库，不丢片
- **通宵韧性**：单任务失败（OOM 等）跳过并记录原因，不中断批次；ComfyUI 连接中断按 60s×30 次自动重试（容忍半夜故障约 30 分钟），仍失败才终止整批；取消在当前片跑完后生效
- **实时进度面板**：完成/失败计数、进度条、当前提示词与种子、预计完成时刻（按含模型维度的实测分桶估算，含自动处理时长）、连接重试状态、失败原因列表、完成提醒
- 自动后处理可选随批执行（插帧/超分/全链），引擎或显存不就绪时跳过并记录，不阻塞批次
- 参数沿用创作台当前选择；批次与日常任务共用 ComfyUI 队列，临时任务可随时插队

## [5.0.0] - 2026-09-18

### 新增
- **模型库**：内置 12 款社区衍生模型目录（官方 INT8/FP8 基线、NVFP4、INT4、W4A8、混合量化、GGUF Q2_K～Q8_0、FastH3 4 步加速版），带画质/速度说明与下载源（hf-mirror 主源 + 官方源回退）
- **本机适配推荐**：读取 ComfyUI 硬件信息，按「模型体积 vs 显存」给出三档推荐（可常驻显存 / 流式加载 / 不建议）
- **一键下载部署**：模型库卡片点下载 → 后台流式下载（进度/速度/ETA 实时显示、断点续传、多源自动回退、磁盘空间预检、下载完原子改名即刻生效），面板重启后中断状态可恢复
- **创作台模型选择**：高级选项新增模型下拉（已装模型 + 推荐标注），作品「复用参数」、参数配方、重复提交检测、长片接龙全线携带模型维度；ETA 实测分桶按模型区分
- **GGUF 支持**：工作流按文件类型自动切换 `UnetLoaderGGUF`（需 ComfyUI 安装 ComfyUI-GGUF，见 DEPLOY-NOTES）；GGUF 组件缺失时目录明确标注
- **加速版权重的特殊处理**：steps_fixed 条目（如 FastH3）自动锁定采样步数，turbo_compat=false 的条目自动跳过 TeaCache 节点
- 模型管理：设为默认（写入 config.json）、删除（保护默认模型与下载中文件）、未知模型文件自动以文件名识别

### 兼容性说明
- 旧数据/旧任务无缝兼容：历史作品元数据无模型字段时按出厂基线模型归档，ETA 分桶照常命中

## [4.3.0] - 2026-08-29

### 新增
- 参数矩阵：「步数扫描」（6+8 / 6+8+10）与批量相乘组成任务组合排队（上限 12）
- 全链配方：后处理新增「插帧 → 超分」链式模式，两级产物（`_rife.mp4` 48fps + `_rife_x2.mp4` 1080p）自动入库；卡片增强菜单、生成后自动处理、参数配方三处入口均可选
- 参数预设更名「参数配方」，保存内容包含自动处理链

### 修复
- v4.2 页面漏拼样式表导致流水线/预设按钮无样式的问题

## [4.2.0] - 2026-08-29

### 新增
- 工作流可视化：生成时显示 9 节点流水线，节点三态（等待/执行中/完成）实时高亮
- 参数预设：保存/应用/删除命名参数组合（localStorage）
- 服务端任务自跟踪：每个任务由后台线程负责完成入库，页面关闭/面板重启均不丢失；重启后自动续接遗留任务

### 修复
- 完成入库依赖浏览器页面轮询的架构缺陷（页面关闭导致任务完成后元数据丢失）

## [4.1.0] - 2026-08-28

### 新增
- AI 增强：RIFE 插帧 ×2（24→48fps）与 Real-ESRGAN 超分 ×2（→1080p），作品卡 🪄 菜单手动触发，或「生成后自动处理」自动执行
- 显存保护：显存不足自动让 ComfyUI 卸载闲置模型
- 队列栏集成后处理任务（可取消）

## [4.0.0] - 2026-08-28

### 新增
- 服务端任务跟踪：任务注册表持久化，页面刷新自动恢复进度，面板重启从 ComfyUI 历史恢复任务元数据
- 队列视图与单任务取消
- 完整参数元数据与一键复用（含采样器/调度器/裁剪）
- 真实耗时统计：按参数组合分桶校准预计耗时；OOM 历史预警
- 失败一键重试
- 作品对比：勾选 2-4 个作品并排同步播放

## [3.x] - 2026-08-27

- UI 主题迭代：深色工作台 → 浅色毛玻璃（v3.3 定稿配方）
- 宽屏自适应布局（主栏 1320px / 作品库 1480px）
- 下拉框样式统一

## [2.x] - 2026-08-24 ~ 08-27

- 文/图生视频、首尾帧定界、长片自动接龙（后端编排）
- 提示词优化（本地规则 + 可选云端 LLM）、提示词语义联动参数
- 批量、取消、历史元数据（搜索/复用/续写/删除/批量管理）
- 显存预算防护、完成提醒、阶段进度条
- v2.5：深色工作台 UI 改版（左侧导航 + 英雄标题 + 玻璃输入台）
